Используя vanzan01 я хочу выполнить задачу по улучшению документации.

1. Расскажи как должна быть организована хорошая документация.
2. Проанализируй раздел "Контекст задачи:" (см. далее по тексту) и дай рекомендации.
3. Дай пошаговую инструкцию что делать, чтобы Cursor 2.3 + vanzan01 улучшил документацию.


Контекст задачи:

1. Есть проект "dns-proxy-nodejs", который проксирует DNS запросы.

Он расположен по адресу https://github.com/ZVanoZ/dns-proxy-nodejs

2. В корне проекта есть файл "readme.md", в котором намешано множество информации.

Например"

* в нем есть секция "## Использование "vanzan01/cursor-memory-bank" в проекте".

Эта информация является вспомогательной и ей не место в корневом файле.

* в нем есть секция "## RAW"

В этой секции консольные команды, которые я использовал при создании проекта.

Они имеют смысл для того, кто хочет сделать аналогичный проект с нуля.

* в нем есть секция "## Как запустить?"

Содержимое этой секции уже устарело.

В этой же секции есть сноска на файл DOCKER-OPTIONS.md, который актуален, но описывает только один из возможных вариантов запуска
```markdown
4. Запуск через Docker или "docker compose" - см. [DOCKER-OPTIONS.md](docs/DOCKER-OPTIONS.md)
```

* есть вспомогательные файлы, которые я использовал для экспериментов.

```text
docs/how-to-run-in-linux.md     - переписка с geminy по поводу запуска приложения в linux
docs/run-app.sh                 - тестовый скрипт, который запускал приложение в linux в PROD режиме (усть еще DEV режим). Скрипт устарел.
```

3. Есть папка "docs/dns" в которой находится спецификация DNS.

Файлы в папке:
```text
01-basics.md
02-packet-structure.md
03-record-types.md
04-resolution-process.md
05-server-types.md
06-response-codes.md
README.md
```

Содержимое этой папки было сгенерировано Cursor по моему запросу. 

Эта документация мне нужна, но я не уверен что ее расположение соответствует общепринятым практикам.


