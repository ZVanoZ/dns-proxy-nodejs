{
  "name": "dns-proxy-nodejs",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node scripts/setup-config.js setup && tsx src/main.ts",
    "build": "node scripts/clean-dist.js && node scripts/build-dependencies.js && node scripts/build-app.js && node scripts/setup-config.js build",
    "start": "node scripts/setup-config.js setup && cd dist && node app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "dns-packet": "^5.6.1",
    "ini": "^6.0.0"
  },
  "devDependencies": {
    "@types/dns-packet": "^5.6.5",
    "@types/ini": "^4.1.1",
    "@types/node": "^25.0.3",
    "esbuild": "^0.24.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
