# Рефлексия: Исправление ошибки биндинга порта 53 в PROD режиме Docker

**Дата:** 2026-01-20  
**Уровень сложности:** Level 2 (Simple Enhancement)  
**Статус:** ✅ Завершено

---

## Резюме

Задача заключалась в том, чтобы устранить ошибку `EACCES` при биндинге порта 53 в PROD режиме при запуске через Docker и при этом сохранить требование: DNS в PROD должен быть доступен на порту 53 для клиентов. В результате:

- PROD контейнер переведен на bridge network с маппингом `53:5053/udp` (порт 53 на хосте → 5053 в контейнере)
- Настроены фиксированные IP адреса контейнеров в подсети `192.168.13.0/24`:
  - `dns-proxy-prod` → `192.168.13.13`
  - `dns-proxy-dev` → `192.168.13.133`
- Обновлены `docker-compose.yml`, `env/prod/Dockerfile`, `env/prod/config/app.ini.dist`, `docs/DOCKER-OPTIONS.md` и `readme.md`
- Подготовлены примеры диагностики DNS запросов и инструкции по настройке DNS в ОС

Решение устранило ошибку биндинга, улучшило безопасность (отказ от `network_mode: host` и `cap_add: NET_BIND_SERVICE`) и добавило стабильные IP адреса для удобства настройки.

---

## Что прошло хорошо

### 1. Четкая локализация проблемы

✅ Ошибка `bind EACCES 0.0.0.0:53` была быстро локализована:
- Логи `env/prod/logs/app-log-2026-01-20.json` показали системную ошибку при биндинге порта 53
- Было подтверждено, что приложение запускается от непривилегированного пользователя `appuser`
- Было учтено, что даже с `NET_BIND_SERVICE` такой сценарий не всегда работает стабильно внутри контейнера

### 2. Выбор безопасного решения

✅ Вместо запуска от root или сложной настройки `setcap` было выбрано решение:
- запустить приложение внутри контейнера на порту 5053
- маппить порт 53 на хосте на 5053 в контейнере (`53:5053/udp`)
- оставить пользователя `appuser`

Это сохранило требования к доступности порта 53 на хосте, но избавило от необходимости работать с привилегированным портом внутри контейнера.

### 3. Фиксация IP адресов контейнеров

✅ Добавление отдельной bridge-сети с ipam-конфигурацией:

```yaml
networks:
  default:
    name: dns-proxy-nodejs-net
    driver: bridge
    ipam:
      config:
        - subnet: 192.168.13.0/24

services:
  dns-proxy-prod:
    networks:
      default:
        ipv4_address: 192.168.13.13

  dns-proxy-dev:
    networks:
      default:
        ipv4_address: 192.168.13.133
```

Позволило:
- Иметь стабильные IP после перезапуска Docker/системы
- Один раз настроить DNS в ОС на `192.168.13.13` (и при необходимости `192.168.13.133` для DEV)
- Использовать эти IP в примерах диагностики.

### 4. Синхронизация документации

✅ Документация обновлена сразу во всех ключевых местах:
- `docs/DOCKER-OPTIONS.md`:
  - Добавлен раздел "Диагностика DNS-запросов"
  - Добавлен раздел "Фиксированные IP адреса для контейнеров"
  - Обновлены примеры запуска с новыми IP и без `--network host` / `--cap-add NET_BIND_SERVICE`
- `readme.md`:
  - Обновлены примеры проверки работоспособности (DEV/PROD/локальный запуск)
  - Добавлено пояснение по использованию фиксированных IP

---

## Вызовы и их решения

### 1. Совмещение требований безопасности и первоначального плана

**Вызов:**
- Первоначальный план требовал, чтобы PROD работал на порту 53 на хосте
- Прямой биндинг 53 внутри контейнера от `appuser` приводил к `EACCES`

**Решение:**
- Разделить "порт внутри контейнера" и "порт на хосте"
- Внутри контейнера использовать 5053 (непривилегированный порт)
- Снаружи (на хосте) экспонировать 53 через маппинг `53:5053/udp`

**Итог:**
- Требование к порту 53 на хосте соблюдено
- Безопасность повышена
- Ошибка биндинга устранена

### 2. Поведение `127.0.0.1` при bridge сети

**Вызов:**
- После перехода на bridge network запросы вида `nslookup google.com 127.0.0.1` перестали работать, что может сбивать с толку

**Решение:**
- Явно задокументировать, что при использовании отдельной bridge сети контейнер не обязан слушать `127.0.0.1`
- Рекомендовать использование фиксированных IP контейнеров (`192.168.13.13` / `192.168.13.133`)
- Добавить соответствующие примеры nslookup в документацию

### 3. Выбор подсети и IP

**Вызов:**
- Нужно было подобрать подсеть, не конфликтующую с существующими сетями

**Решение:**
- Использовать `192.168.13.0/24` как отдельную подсеть для Docker сети
- Добавить в документацию рекомендацию проверять конфликты через `docker network ls`, `docker network inspect` и `ip addr`

---

## Уроки

### 1. Логирование как источник правды

Логи `env/prod/logs/app-log-2026-01-20.json` сыграли ключевую роль:
- Показали точную ошибку (`EACCES`) и контекст (порт, адрес)
- Подтвердили успешный запуск после исправлений
- Продемонстрировали успешные ответы DNS (google.com, rm.dio.local)

👉 **Урок:** При работе с сетевыми ошибками и привилегиями всегда начинать с анализа логов приложения и контейнера.

### 2. Отделение внутренней и внешней конфигураций

Разделение:
- Внутреннего порта (5053)
- Внешнего порта (53)
- Логики приложения (OptionsLoader + app.ini)
- Инфраструктурных настроек (docker-compose, сеть)

упростило решение задачи без изменения бизнес-логики приложения.

👉 **Урок:** Не стоит "ломать" приложение ради инфраструктурной задачи, если проблему можно решить на уровне окружения (Docker, сеть, маппинг портов).

### 3. Фиксированные IP как UX-улучшение

Добавление фиксированных IP адресов:
- Упростило настройку DNS на рабочих станциях
- Убрало необходимость каждый раз искать IP контейнера
- Сделало документацию более практичной

👉 **Урок:** Для инфраструктурных сервисов (DNS, прокси, БД) фиксированные IP в рамках отдельной Docker сети часто удобнее, чем динамические адреса.

---

## Улучшения процесса

### 1. Шаблон для задач "Docker + сеть"

Опыт этой задачи можно оформить в виде шаблона:
- Анализ проблемы (логи, порты, пользователи, capabilities)
- Выбор стратегии (host vs bridge vs macvlan)
- Решение с минимальными изменениями в приложении
- Обновление документации и примеров диагностики

### 2. Стандартизация подсетей

Имеет смысл:
- Ввести стандартные подсети для разных проектов (например, `192.168.13.0/24` для DNS-прокси)
- Документировать их в `techContext.md` или отдельном файле инфраструктурных соглашений

### 3. Автоматические проверки конфигурации

Полезно добавить:
- Скрипт/чеклист проверки:
  - `docker compose config`
  - Проверка подсетей на конфликты
  - Проверка доступности портов

---

## Технические улучшения

### 1. docker-compose.yml

- Переход на bridge network с ipam-конфигурацией
- Фиксированные IP для PROD и DEV контейнеров
- Явный маппинг портов `53:5053/udp` для PROD

### 2. Dockerfile (env/prod/Dockerfile)

- EXPOSE приведен в соответствие с фактическим портом (5053/udp)
- Приложение по-прежнему запускается от `appuser`

### 3. Конфигурация (env/prod/config/app.ini.dist)

- Порт 5053 подтвержден как стандарт для PROD внутри контейнера

### 4. Документация

- `docs/DOCKER-OPTIONS.md` и `readme.md` синхронизированы с новой конфигурацией
- Добавлены практические примеры nslookup для DEV/PROD/локального запуска
- Описаны шаги по настройке DNS в ОС

---

## Сравнение с планом

**План:**
1. Обновление docker-compose.yml для PROD
2. Обновление app.ini.dist для PROD
3. Обновление Dockerfile
4. Обновление документации
5. Тестирование

**Фактическое выполнение:**
- Все шгапы выполнены строго по плану
- Дополнительно реализованы:
  - Настройка фиксированных IP адресов
  - Расширенная документация по диагностике

---

## Следующие шаги

1. Наблюдать за использованием новой конфигурации в реальных сценариях
2. При необходимости расширить документацию разделом "Типичные ошибки и их решения" для DNS-прокси
3. При добавлении новых окружений (например, STAGE) использовать ту же сетевую модель (bridge + фиксированные IP)

---

## Заключение

Задача по исправлению ошибки биндинга порта 53 в PROD режиме Docker была успешно решена с учетом:
- требований к доступности порта 53 для клиентов
- требований безопасности
- удобства эксплуатации (фиксированные IP, обновленная документация)

**Общая оценка выполнения:** ⭐⭐⭐⭐⭐ (5/5)

Решение масштабируемо, безопасно и хорошо задокументировано, что упрощает дальнейшую поддержку и развитие проекта.
