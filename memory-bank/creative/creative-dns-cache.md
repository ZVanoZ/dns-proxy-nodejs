# Creative Phase: Кеширование DNS-запросов и структура answer-source

**Дата создания:** 2026-01-20  
**Тип:** Архитектурное решение  
**Статус:** 🎨 CREATIVE PHASE ✅ **РЕШЕНИЕ ПРИНЯТО**

---

## 🎨🎨🎨 ENTERING CREATIVE PHASE: DNS CACHE ARCHITECTURE

### Требования

#### Функциональные требования
1. **Кеширование DNS-запросов:**
   - Кешировать DNS-ответы от upstream-серверов
   - Учитывать TTL из ответов DNS
   - Поддерживать отрицательное кеширование (NXDOMAIN)
   - Ограничение размера кеша для защиты от утечек памяти

2. **Логирование источников ответов:**
   - Добавить структуру `answer-source` в логи
   - Отслеживать источник каждого DNS-ответа
   - Поддерживать источники: cache, hosts, upstream (ip:port)

#### Технические ограничения
- TypeScript strict mode, строгая типизация
- ООП подход (классы, инкапсуляция)
- Существующие паттерны (Factory, Service)
- Совместимость с текущей логикой обработки запросов
- Потокобезопасность (UDP-запросы могут приходить параллельно)

---

## Анализ вариантов решения

### Вариант 1: In-Memory Cache с Map (базовый)

**Описание:**
- Класс `DnsCache` с `Map<string, CacheEntry>`
- Ключ: `dnsName` (например, "example.com")
- Значение: `CacheEntry` с ответом, временем создания и TTL
- Автоматическая очистка устаревших записей при проверке

**Плюсы:**
- ✅ Простая реализация
- ✅ Низкие накладные расходы
- ✅ Быстрый доступ (O(1))
- ✅ Легко интегрируется в `App`

**Минусы:**
- ❌ Нет ограничения размера (риск утечки памяти)
- ❌ Нет LRU-эвакуации при переполнении

**Оценка:** ⭐⭐⭐⭐ (4/5)

---

### Вариант 2: In-Memory Cache с LRU и ограничением размера ⭐ **ВЫБРАНО**

**Описание:**
- Расширение Варианта 1 с LRU-эвакуацией
- Ограничение размера кеша (например, 1000 записей)
- При переполнении удаляется самая старая запись

**Плюсы:**
- ✅ Все плюсы Варианта 1
- ✅ Защита от утечки памяти
- ✅ Предсказуемое использование памяти
- ✅ Автоматическая эвакуация старых записей

**Минусы:**
- ❌ Сложнее реализация (нужен LRU)
- ❌ Дополнительные накладные расходы на поддержание порядка

**Оценка:** ⭐⭐⭐⭐⭐ (5/5) - **РЕКОМЕНДУЕТСЯ**

---

### Вариант 3: Кеш с периодической очисткой (Background Cleanup)

**Описание:**
- Вариант 2 + фоновый таймер для очистки устаревших записей
- Периодическая проверка и удаление истекших записей

**Плюсы:**
- ✅ Все плюсы Варианта 2
- ✅ Автоматическая очистка устаревших записей
- ✅ Меньше проверок при каждом запросе

**Минусы:**
- ❌ Дополнительная сложность (таймеры)
- ❌ Дополнительное использование ресурсов
- ❌ Нужна корректная остановка при завершении приложения

**Оценка:** ⭐⭐⭐⭐ (4/5) - избыточно для начальной версии

---

### Вариант 4: Кеш с персистентностью (Redis/File-based)

**Описание:**
- Внешнее хранилище (Redis) или файловый кеш
- Сохранение кеша между перезапусками

**Плюсы:**
- ✅ Персистентность
- ✅ Возможность масштабирования

**Минусы:**
- ❌ Сложность реализации
- ❌ Дополнительные зависимости
- ❌ Больше накладных расходов
- ❌ Избыточно для текущих требований

**Оценка:** ⭐⭐ (2/5) - избыточно

---

## Рекомендуемое решение: Вариант 2 (LRU Cache)

**Обоснование:**
1. Баланс простоты и функциональности
2. Защита от утечки памяти через ограничение размера
3. Соответствие архитектуре проекта (ООП, строгая типизация)
4. Легкая интеграция в существующий код
5. Возможность расширения в будущем (статистика, персистентность)

---

## Структура answer-source

### Определение структуры

```typescript
interface AnswerSource {
  'masked-dns': string | null;  // Значение из секции [masked-dns]
  'real-source': 'cache' | 'hosts' | string;  // string для формата "ip:port"
}
```

### Источники ответов

1. **"hosts"** - из секции `[hosts]` конфига
   - `masked-dns: null`
   - `real-source: "hosts"`

2. **"cache"** - из кеша DNS-запросов
   - `masked-dns: chainName` (определяется по dnsName)
   - `real-source: "cache"`

3. **"ip:port"** - из upstream DNS сервера
   - `masked-dns: chainName`
   - `real-source: "10.10.10.1:53"`

---

## Архитектурные решения

### 1. Модуль DnsCache

**Структура:**
- `src/DnsCache/types.ts` - Типы и интерфейсы
- `src/DnsCache/DnsCache.ts` - Основной класс
- `src/DnsCache/index.ts` - Экспорт API

**Ключевые компоненты:**
- `CacheEntry` - запись в кеше
- `DnsCacheInterface` - интерфейс кеша
- `DnsCache` - реализация с LRU

### 2. Интеграция с App

**Изменения:**
- Добавить поле `dnsCache: DnsCacheInterface | null`
- Метод `setDnsCache(cache: DnsCacheInterface): void`
- Модифицировать `getIp()` для использования кеша
- Модифицировать `sendSuccessResponse()` для логирования `answer-source`

### 3. Конфигурация

**Секция в app.ini:**
```ini
[dns-cache]
enabled=true
max-size=1000
max-ttl=86400
negative-ttl=60
```

---

## Обработка edge cases

1. **TTL = 0:** не кешировать (проверка в `set` и `setNegative`)
2. **Очень большие TTL:** ограничивать максимальным значением (например, 86400)
3. **Параллельные запросы:** Map в Node.js потокобезопасен для чтения/записи в одном процессе
4. **Пустые ответы:** обрабатывать как отрицательный кеш
5. **Ошибки парсинга TTL:** использовать значение по умолчанию (300 секунд)

---

## Метрики и мониторинг

- Статистика кеша: hits, misses, negative hits, размер
- Логирование: попадания/промахи кеша (debug уровень)
- Метрики производительности: время ответа с/без кеша

---

## 🎨🎨🎨 EXITING CREATIVE PHASE

**Принятые решения:**
- ✅ Вариант 2: In-Memory Cache с LRU и ограничением размера
- ✅ Структура `answer-source` для логирования источников ответов
- ✅ Интеграция через метод `setDnsCache` в классе `App`
- ✅ Конфигурируемость через секцию `[dns-cache]` в `app.ini`

**Следующие шаги:**
- Перейти к `/plan` для детального планирования
- Реализовать через `/build`
