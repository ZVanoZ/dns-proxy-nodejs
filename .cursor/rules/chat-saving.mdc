---
description: Автоматическое журналирование чатов Cursor AI
globs: chat-saving.mdc
alwaysApply: true
---

# АВТОМАТИЧЕСКОЕ ЖУРНАЛИРОВАНИЕ ЧАТОВ

🚨 **КРИТИЧЕСКОЕ ПРАВИЛО: ОБЯЗАТЕЛЬНОЕ ЖУРНАЛИРОВАНИЕ ВСЕХ ЧАТОВ** 🚨

Это правило является частью методологии vanzan01/cursor-memory-bank v0.8 и должно выполняться ВСЕГДА при работе в любом чате.

## ОБЯЗАТЕЛЬНЫЕ ДЕЙСТВИЯ ПРИ НАЧАЛЕ РАБОТЫ В ЧАТЕ

### 1. Создание файла журнала при первом запросе

* Если файл нельзя создать, то самостоятельно перейти в режим Agent.
* Если автоматически сменить режим нельзя, то попросить пользователя сменить режим на Agent и выполнить команду `/chat-save`.

При обработке **ПЕРВОГО запроса пользователя в новом чате** ВСЕГДА:

1. **Определить путь к файлу журнала:**
   - Получить текущее время по Гринвичу (GMT/UTC).
   - Сформировать путь: `./docs/prompt/<YYYY>/<YYYY-MM-DD>/<HH24-MM>/chat.md`
   - Пример: `./docs/prompt/2026/2026-01-16/14-30/chat.md` (для чата, созданного 16 января 2026 года в 14:30 UTC)

2. **Создать директории и файл:**
   - Создать все необходимые директории (рекурсивно) если они не существуют
   - Создать пустой файл `chat.md` если он не существует
   - Если файл уже существует, использовать его (не перезаписывать)

3. **Инициализировать файл журнала:**
   - Если файл был только что создан (пустой), добавить заголовок:
     ```markdown
     # Журнал чата
     
     Чат создан: <YYYY-MM-DD HH24:MI:SS GMT>
     
     ---
     ```
   - Режим работы НЕ записывается в заголовок, а записывается для каждого запроса отдельно

## ОБЯЗАТЕЛЬНЫЕ ДЕЙСТВИЯ ПРИ КАЖДОМ ЗАПРОСЕ

### 2. Экспорт чата при старте запроса

При **НАЧАЛЕ обработки КАЖДОГО запроса пользователя**:

1. **Проверить существование файла журнала:**
   - Если файл не существует, создать его по правилам из раздела 1
   - Определить путь к файлу на основе времени первого запроса в чате (или текущего времени если это первый запрос)

2. **Экспортировать текущее состояние чата:**
   - Определить режим работы для текущего запроса (Ask mode / Agent mode / другой режим)
   - Для каждого предыдущего запроса определить его режим работы (если не был определен ранее)
   - Прочитать все предыдущие сообщения из контекста чата
   - Сформировать полное содержимое чата в формате Markdown
   - **Полностью заменить** содержимое файла журнала этим содержимым

3. **Формат экспорта при старте запроса:**
   ```markdown
   # Журнал чата
   
   Чат создан: <дата и время создания GMT>
   
   ---
   ## Запрос: <время первого запроса GMT>
   Режим работы: <режим работы для первого запроса>
   [Первый запрос пользователя]
   
   [Ответ AI]
   
   ---
   ## Запрос: <время второго запроса GMT>
   Режим работы: <режим работы для второго запроса>
   [Второй запрос пользователя]
   
   [Ответ AI]
   
   ---
   ## Запрос: <время текущего запроса GMT>
   Режим работы: <режим работы для текущего запроса>
   [Текущий запрос пользователя]
   
   [Ответ AI формируется...]
   ```

### 3. Экспорт чата при завершении запроса

При **ЗАВЕРШЕНИИ обработки КАЖДОГО запроса пользователя** (после формирования ответа):

1. **Экспортировать полное состояние чата:**
   - Определить режим работы для текущего запроса (Ask mode / Agent mode / другой режим)
   - Для каждого предыдущего запроса определить его режим работы (если не был определен ранее)
   - Включить все сообщения чата, включая только что сформированный ответ
   - Сформировать полное содержимое чата в формате Markdown
   - **Полностью заменить** содержимое файла журнала этим содержимым

2. **Формат экспорта при завершении запроса:**
   ```markdown
   # Журнал чата
   
   Чат создан: <дата и время создания GMT>
   
   ---
   ## Запрос: <время первого запроса GMT>
   Режим работы: <режим работы для первого запроса>
   [Первый запрос пользователя]
   
   [Ответ AI]
   
   ---
   ## Запрос: <время второго запроса GMT>
   Режим работы: <режим работы для второго запроса>
   [Второй запрос пользователя]
   
   [Ответ AI]
   
   ---
   ## Запрос: <время текущего запроса GMT>
   Режим работы: <режим работы для текущего запроса>
   [Текущий запрос пользователя]
   
   [Полный ответ AI]
   ```

## ФОРМАТ ФАЙЛА ЖУРНАЛА

### Структура сообщений

Каждое сообщение должно быть оформлено следующим образом:

```markdown
---
## Запрос: <YYYY-MM-DD HH24:MI:SS GMT>
Режим работы: <режим работы для этого запроса>
[Текст запроса пользователя]

[Текст ответа AI]
---
```

### Временные метки

- Все временные метки должны быть в формате GMT/UTC
- Формат: `YYYY-MM-DD HH24:MI:SS GMT`
- Пример: `2026-01-16 14:30:15 GMT`

### Режим работы чата

- **ОБЯЗАТЕЛЬНО** записывать режим работы для КАЖДОГО запроса отдельно
- Режим работы НЕ записывается в заголовок файла, а записывается для каждого запроса
- Возможные режимы: `Ask mode`, `Agent mode`, или другой режим если применимо
- Режим работы для каждого запроса определяется на основе доступных инструментов и возможностей AI в момент обработки этого запроса
- Формат: `Режим работы: <режим работы для этого запроса>`
- Пример: `Режим работы: Agent mode` или `Режим работы: Ask mode`
- Разные запросы в одном чате могут иметь разные режимы работы

### Разделители

- Использовать `---` (три дефиса) для разделения запросов
- Использовать заголовок `## Запрос: <время>` для каждого запроса

## АЛГОРИТМ РАБОТЫ

### Шаг 1: Определение пути к файлу журнала

```javascript
// Псевдокод для определения пути
const now = new Date();
const year = now.getUTCFullYear();
const month = String(now.getUTCMonth() + 1).padStart(2, '0');
const day = String(now.getUTCDate()).padStart(2, '0');
const hour = String(now.getUTCHours()).padStart(2, '0');
const minute = String(now.getUTCMinutes()).padStart(2, '0');

const logPath = `./docs/prompt/${year}/${year}-${month}-${day}/${hour}-${minute}/chat.md`;
```

### Шаг 2: Создание файла (если не существует)

1. Проверить существование файла
2. Если не существует:
   - Создать директории рекурсивно
   - Создать пустой файл
   - Добавить заголовок с временем создания (без режима работы)

### Шаг 3: Экспорт при старте запроса

1. Определить режим работы для текущего запроса (Ask mode / Agent mode / другой режим)
2. Для каждого предыдущего запроса определить его режим работы (если не был определен ранее)
3. Прочитать все сообщения из контекста чата
4. Сформировать Markdown содержимое с указанием режима работы для каждого запроса
5. Добавить текущий запрос пользователя с указанием его режима работы (без ответа)
6. Полностью заменить содержимое файла

### Шаг 4: Экспорт при завершении запроса

1. Определить режим работы для текущего запроса (Ask mode / Agent mode / другой режим)
2. Для каждого предыдущего запроса определить его режим работы (если не был определен ранее)
3. Прочитать все сообщения из контекста чата
4. Включить только что сформированный ответ
5. Сформировать полное Markdown содержимое с указанием режима работы для каждого запроса
6. Полностью заменить содержимое файла

## КРИТИЧЕСКИЕ ТРЕБОВАНИЯ

1. **ВСЕГДА создавать файл журнала** при первом запросе в чате
2. **ВСЕГДА определять и записывать режим работы для КАЖДОГО запроса** отдельно (не в заголовок)
3. **ВСЕГДА экспортировать чат** при старте каждого запроса
4. **ВСЕГДА экспортировать чат** при завершении каждого запроса
5. **ВСЕГДА использовать время GMT/UTC** для формирования пути и временных меток
6. **ВСЕГДА полностью заменять** содержимое файла при экспорте (не добавлять, а заменять)
7. **ВСЕГДА создавать директории** если они не существуют
8. **ВСЕГДА определять режим работы** для каждого запроса на основе доступных инструментов в момент обработки этого запроса

## ПРОВЕРКА ВЫПОЛНЕНИЯ

После каждого запроса проверять:
- ✅ Файл журнала существует по правильному пути
- ✅ Каждый запрос содержит режим работы для этого запроса
- ✅ Файл содержит все сообщения чата
- ✅ Временные метки в формате GMT
- ✅ Формат соответствует требованиям

## ИСКЛЮЧЕНИЯ

НЕТ ИСКЛЮЧЕНИЙ. Это правило должно выполняться для ВСЕХ чатов без исключений.
